import {createContext} from "react";

export const HasDBConnectionProvider = createContext(false);
export const IsEnvSettingValidProvider = createContext(false);
export const IsInstallationLockedProvider = createContext(false);

